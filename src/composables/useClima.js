export default function useClima() {
  const getClima = ({ ciudad, pais }) => {
    // importar el api key
    const key = import.meta.env.VITE_API_KEY;

    // Obtener la lat , lng

    //Obtener el clima
  };

  return {
    getClima,
  };
}
